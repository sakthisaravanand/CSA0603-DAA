#include<stdio.h>
#include<string.h>
int main(){
	int i,len;
	char s1[100];
	printf("Enter the string: ");
	scanf("%s",s1);
	len=strlen(s1);
	for(i=len-1;i>=0;i--){
		printf("  %c",s1[i]);
	}
	return 0;
}
